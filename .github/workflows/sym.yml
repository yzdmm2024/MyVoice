name: Sym-Build (任意 ref · 未 strip dylib)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'git ref / full sha to build'
        required: true
        default: main
      label:
        description: 'artifact label (version name)'
        required: true
        default: sym

jobs:
  build:
    runs-on: macos-14
    env:
      THEOS: ${{ github.workspace }}/theos
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - name: Install deps
        run: brew install dpkg ldid
      - name: Setup theos
        run: git clone --recursive https://github.com/theos/theos.git "$THEOS"
      - name: Vendor Flite (best effort)
        run: bash flite/vendor.sh || true
      - name: Build with release flags (FINALPACKAGE=1)
        run: make package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless || true
      - name: Collect unstripped dylib + deb
        if: always()
        run: |
          mkdir -p out
          find .theos -name 'MyVoice.dylib' -exec cp -v {} out/ \;
          cp -v packages/*.deb out/ 2>/dev/null || true
          ls -la out || true
      - uses: actions/upload-artifact@v4
        with:
          name: sym-${{ inputs.label }}
          path: out/*
          if-no-files-found: warn
