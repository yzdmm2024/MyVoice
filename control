Package: com.yzdmm2024.myvoice
Name: 我的语音
Version: 2.2.8
Architecture: iphoneos-arm64
Description: ★ 2.2.8 【修复启动闪退 + UI 紧凑化】① 闪退：崩溃日志（iOS16.6/微信8.0.75.33）指向主线程 SIGSEGV @0x90、selector=sharedInstance，反汇编 MyVoice.dylib 的 dyld initializer（offset 0x80F0）证实链路为 %ctor → [[MyVoiceManager shared] setup] → [MyVoicePanel show] → refreshSession → MVService/NSClassFromString(宿主类) → 触发微信 +initialize（此刻微信自己的初始化还没跑完）→ 取到未就绪单例 → 崩。修法：%ctor 内只做与宿主完全无关的事（写日志/入队）；面板装配延后到 UIApplicationDidFinishLaunchingNotification（+1.5s 兜底轮询）；新增宿主就绪探针 MVHostReady()（只用系统类 UIApplication，绝不用 NSClassFromString(宿主类) 判定）并给 MVService / classWithCandidates 加守卫。② UI：面板高度 420→380、主视图纵向间距重排，消除底部约 104pt 空白；修音色选择页标题与「返回」按钮完全重叠（标题 x14→234 vs 返回 x140→220）的问题，改为标题收窄 + 按钮右对齐，并给标题/音色条加自动缩字号兜底。 ★ 2.2.7 【发送提速】真机日志把延迟拆开量过了：点发送→合成返回 0.89s、启动录音→首个回调 0.39s、喂入 2.00s（＝音频时长）、收尾 0.25s，合计 3.57s。其中「喂入」是录音管线实时性的物理下限（2 秒语音必须占用 2 秒真实时间，静音裁剪实测只省 70ms），真正白等的是**合成那 0.89s 网络往返**。本版把它归零：① 面板里文字一改就（0.35s 防抖后）在后台预合成并缓存，点「发送」直接命中，合成耗时 = 0；② 打开面板时预热 DNS+TLS 连接（实测首次合成约 0.3~0.4s 花在握手上），连接复用后未命中的合成也更快；③ 每次发送落盘诊断 wav 改到后台写，不再占发送关键路径；④ 新增 [perf] 分段耗时日志（合成网络 / 解码 / 点发送→拿到 PCM），下次再慢可直接看出卡在哪一段。另外说明：想过更快就调面板上的「语速」滑块（0.5~2.0x）——语速 1.0×→1.3× 会让 2.0s 的语音变成约 1.5s，发送随之快约 0.5s。 ★ 2.2.6 【根治卡顿/变调】真机日志定位到云端 wav→PCM 用了 AVAudioConverter，其 withInputFromBlock 会把同一个输入块重复供给转换器 → 输出里插重复帧（听感就是"一卡一卡/两个声音"），且采样率/时长不自洽。现改为**自己解析 RIFF 头 + 自己线性重采样**，全流程确定，并保证输出恒为 16kHz 单声道（日志会打印 wav 的真实 tag/声道/采样率/位深）。另外：发送收尾余量 0.35s→0.25s；新增回调节奏诊断（每次发送记录每块字节与间隔，判断管线是否有缺口）；每次发送把真正喂入管线的音频另存为沙盒 Documents/MyVoice_last.wav，便于直接试听核对。 ★ 2.2.5 等 TTS 喂完再松手 + 单一数据流/精确绑定单队列（修延迟与重音）。 对齐业界已验证实现（TTSFloat v29），修「有延迟/好久都不出来」与「重音」：① 松手时机改为等【TTS 真正喂完】再调 StopRecord —— 旧版按预估时长定时停会把音频截断，微信一直等完整数据 → 转圈、半截语音、好久不出气泡；② 录音注入回到【单一数据流】（全局一份喂入偏移），并新增【精确绑定】：只替换本次启动录音后新建的那一个队列，其它输入队列原样不动 —— 旧版让每个队列各喂一份完整 TTS，两条流同时说同一句话就是叠音/重音；③ 发送收尾后自动解除装填，避免残留状态把之后的真实录音注成静音。微信文字转语音（阿里云百炼：千问 Qwen-TTS 预置音色 / CosyVoice 克隆音色）。 ★ 2.2.4 自动发送改为【无界面直发】：直接调微信内部录音接口（RecordController StartRecordingFromUsr:ToUsr:UserInfo: / AudioSender StopRecord），全程不弹「按住说话」界面、无需按住；设置→我的语音里新增开关可一键关回手动按住。 ★ 2.2.3 新增「自动发送」。★ 2.2.2 缺 API Key 时明确报错不再静默回退。 ★ 2.2.0 新增千问 Qwen-TTS 云端服务（免克隆，与 CosyVoice 共用同一 API Key）。 ★ 2.1.0 起为【录音管线劫持】：合成好的 PCM 装填进缓冲，录音回调里把麦克风数据整块替换成 TTS，松手后由微信自己完成 SILK 编码、落盘、入库、上传与气泡 —— 语音必定可播放、时长由微信实算。采样率按录音管线实测校正为 16kHz。用法：在目标聊天页点面板「发送」，自动发出去（或手动按住说话键，不用出声）。 自动发送改为【无界面直发】：点「发送」后直接调微信内部录音接口（RecordController StartRecordingFromUsr:ToUsr:UserInfo: / AudioSender StopRecord），全程不弹「按住说话」界面、无需按住、也不会再出现「界面收不起来」；设置→我的语音里新增开关，可一键关掉自动发送回到手动按住。修复「语音有回音/听起来两个声音」：多录音队列原本共用一份喂入偏移，会互相抢食并反复从头重放 TTS，现改为每个队列各自一份独立偏移与重采样副本。★ 2.2.3 新增「自动发送」：装填完成后自动模拟按住说话并松手，无需手动按住（默认开启；自动触发未生效会自动回退手动提示）。发送前停止预览朗读，避免预览外放与录音注入串音造成的重叠。微信文字转语音（阿里云百炼：CosyVoice 克隆音色 / 千问 Qwen-TTS 预置音色）。★ 2.2.2 修复「云端模式缺 API Key 时静默回退本地 AVS、报看不懂的 AVS无输出」问题——现在会明确提示去设置填 DashScope Key；本地 AVS 引擎加中文语音预检，未下载中文语音时给出清晰指引。微信文字转语音（阿里云百炼：CosyVoice 克隆音色 / 千问 Qwen-TTS 预置音色）。微信文字转语音（阿里云百炼：CosyVoice 克隆音色 / 千问 Qwen-TTS 预置音色）。★ 2.2.0 新增千问 Qwen-TTS 云端服务（官方预置音色，免克隆，与 CosyVoice 共用同一 API Key，设置里一键切换）；修复录音队列只绑定第一个导致的替换失效（发出去的是麦克风原声而非 TTS）；合成失败不再静默回退蜂鸣占位音，改为明确报错。2.1.0 已改为【录音管线劫持】：合成好的 PCM 装填进缓冲，用户在原聊天页按住「按住 说话」时把麦克风采样整块替换成 TTS 数据，松手后由微信自己完成 SILK 编码、落盘、入库、上传与气泡。★ 2.1.0 发送链路重做：2.0.17 的「自建 CMessageWrap + AddMsg 直塞」虽然能出气泡，但音频没被微信的录音/上传链注册，播放出来是空白无声。现改为【录音管线劫持】：合成好的 PCM 装填进缓冲，用户在原聊天页按住「按住 说话」时把麦克风采样整块替换成 TTS 数据，松手后由微信自己完成 SILK 编码、落盘、入库、上传与气泡 —— 因此语音必定可播放、时长由微信实算。采样率按录音管线实测校正为 16kHz（原 24kHz 会造成音调/时长错位）。用法：①在目标聊天页点面板「① 合成语音」→ ②自动发送（或手动按住说话键，不用出声）→ ③松开发送。
Maintainer: yzdmm2024
Author: yzdmm2024
Section: Tweaks
Depends: firmware (>=14.0), mobilesubstrate (>= 0.9.7100) | com.rpetrich.ellekit
